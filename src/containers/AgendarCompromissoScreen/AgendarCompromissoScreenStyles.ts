import { ViewStyle } from 'react-native';
import { ApplicationStyles } from '../../themes';

export default {
	...ApplicationStyles,
	mainContainer: {
		paddingTop: 38,
		paddingHorizontal: 20
	} as ViewStyle,
};
