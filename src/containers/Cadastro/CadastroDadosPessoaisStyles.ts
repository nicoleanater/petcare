import { ViewStyle } from 'react-native';
import { ApplicationStyles } from '../../themes';

export default {
		...ApplicationStyles,
		mainContainer: {
			paddingVertical: 25,
			alignItems: 'center'
		} as ViewStyle
};